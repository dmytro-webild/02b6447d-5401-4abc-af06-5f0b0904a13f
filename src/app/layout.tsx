import type { Metadata } from "next";
import { Halant } from "next/font/google";
import { Inter } from "next/font/google";
import "./globals.css";
import "@/lib/gsap-setup";
import { ServiceWrapper } from "@/components/ServiceWrapper";
import Tag from "@/tag/Tag";
import { getVisualEditScript } from "@/utils/visual-edit-script";
import { Nunito } from "next/font/google";



export const metadata: Metadata = {
  title: 'NexGame | Next Generation Gaming Platform',
  description: 'Experience next-gen competitive gaming on the premier NexGame platform. Discover immersive RPGs, tactical shooters, and a growing community.',
  keywords: ["gaming, rpg, tactical shooter, online games, mmo, esports, play now"],
  openGraph: {
    "title": "NexGame Platform",
    "description": "The next evolution of competitive gaming.",
    "siteName": "NexGame",
    "type": "website"
  },
};

const nunito = Nunito({
  variable: "--font-nunito",
  subsets: ["latin"],
});

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <ServiceWrapper>
        <body className={`${nunito.variable} antialiased`}>
          <Tag />
          {children}
          <script
              dangerouslySetInnerHTML={{
                  __html: `${getVisualEditScript()}`
              }}
          />
        </body>
      </ServiceWrapper>
    </html>
  );
}
